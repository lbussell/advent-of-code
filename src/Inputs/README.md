# Inputs

Place inputs in this directory with the filename `N.txt` where `N` is the number
of the day's challenge.

Any text files in this folder can be referenced from any of the projects in the
solution, but only inputs matching `?.txt` will be ignored by git. This allows
some public test inputs to be stored in version control, but not the secret user
specific inputs.
